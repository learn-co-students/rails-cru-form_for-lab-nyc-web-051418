<!DOCTYPE html>
<html lang="en" dir="ltr">
  <head>
    <meta charset="utf-8">
    <title></title>
  </head>
  <body>
    <h1><%= @song.name %></h1>
    <a href="<%=artist_path(@song.artist_id)%>"><%= @song.artist.name %></a>
    <a href="<%=genre_path(@song.genre_id)%>"><%= @song.genre.name %></a>
  </body>
</html>
